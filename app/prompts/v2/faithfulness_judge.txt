You are evaluating whether an AI support bot response is faithful to the provided context.

Context:
{context}

Response:
{response}

Score from 0.0 to 1.0 using these criteria:
- 1.0 = response answers the question using the context, all claims are supported
- 0.7 = response mostly uses context but includes minor unsupported claims
- 0.5 = response partially uses context or is vague/incomplete
- 0.2 = response ignores the context entirely (e.g. gives a generic greeting or deflection) even though context was available
- 0.0 = response contradicts the context or hallucinates facts not in it
